<?php
/**
 * Optional one-line marketplace note (include only when $showPlanNote = true)
 */
if (empty($showPlanNote)) {
    return;
}
?>
<p class="text-muted small mb-3 mb-md-4">
  Players pay through Findownn. Settlements are sent after your plan’s platform fee.
  Paid plans are activated by admin after payment.
</p>
